FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Don't copy source code - will be mounted via volume

# Expose port
EXPOSE 3000

# Start development server listening on all interfaces
CMD ["npm", "run", "dev", "--", "-H", "0.0.0.0"]
